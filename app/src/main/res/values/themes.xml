<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.RentCalendar" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/purple_700</item>
    </style>

    <style name="Theme.CustomSplashScreenTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/payback</item>
        <item name="postSplashScreenTheme">@style/Theme.RentCalendar</item>
    </style>
</resources>